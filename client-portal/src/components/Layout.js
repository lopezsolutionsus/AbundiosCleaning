import { NavLink, Outlet } from 'react-router-dom';

const CalendarIcon = () => (
  <svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
    <rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/>
  </svg>
);

const HomeIcon = () => (
  <svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
    <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/>
  </svg>
);

const QuoteIcon = () => (
  <svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
    <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/><polyline points="10 9 9 9 8 9"/>
  </svg>
);

const LogoutIcon = () => (
  <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
    <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/><polyline points="16 17 21 12 16 7"/><line x1="21" y1="12" x2="9" y2="12"/>
  </svg>
);

export default function Layout() {
  const firstName = localStorage.getItem('first_name') || 'there';

  function logout() {
    localStorage.clear();
    window.location.href = '/login.html';
  }

  return (
    <div className="layout">
      <aside className="sidebar">
        <a href="https://abundioscleaning.com" className="sidebar-logo">Abundios Cleaning</a>
        <div className="sidebar-greeting">Hi, {firstName}</div>
        <nav>
          <NavLink to="/dashboard"><CalendarIcon /> My Appointments</NavLink>
          <NavLink to="/properties"><HomeIcon /> My Properties</NavLink>
          <NavLink to="/quote"><QuoteIcon /> Request a Quote</NavLink>
        </nav>
        <button className="sidebar-logout" onClick={logout}><LogoutIcon /> Log out</button>
      </aside>
      <main className="main-content">
        <Outlet />
      </main>
    </div>
  );
}
